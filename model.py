import numpy as np

def Erf(x):
    return

def BehrensSim (S0, f, bval, bvecs, d, theta, phi):
    #if ()
    sum_f = sum(f)
    stick = sum(f) *
    ball = (1 - sum_f)

    co = ball + stick

    Si = S0 * co


    return Si

